<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title><%= org.name %> · Agendador</title>
  <link rel="stylesheet" href="<%= cdn %>/styles.css"/>
</head>
<body>
  <main class="wrap">
    <div class="topbar">
      <div class="brand">
        <% if (org.logo_url) { %>
          <img src="<%= org.logo_url %>" alt="<%= org.name %>"/>
        <% } else { %>
          <div class="badge">/<%= org.slug %></div>
        <% } %>
        <div>
          <h1 style="margin:0"><%= org.name %></h1>
          <div class="muted">Agenda online</div>
        </div>
      </div>
      <div class="actions">
        <button id="openLogin" class="btn primary">Reservar / Iniciar sesión</button>
        <button id="logoutBtn" class="btn ghost" title="Cerrar sesión">Salir</button>
      </div>
    </div>

    <section class="grid">
      <div class="hero card">
        <h2 style="margin:0 0 8px">Reserva tu cita en 10 segundos</h2>
        <p class="muted">Te verificamos por WhatsApp o por email. Sin contraseñas, sin dramas.</p>
        <div class="divider"></div>
        <div class="inline">
          <span class="badge">Estado WhatsApp</span>
          <span class="muted">Si no recibes el código por WhatsApp, escanea el QR:</span>
          <a class="btn" href="/whatsapp/qr" target="_blank">Ver QR</a>
          <a class="btn" href="/qr.png" target="_blank">/qr.png</a>
        </div>
      </div>

      <div class="card">
        <h3 style="margin-top:0">Próximas citas</h3>
        <div class="list"><div class="item muted">Inicia sesión para ver tus próximas reservas. (Placeholder)</div></div>
      </div>
    </section>

    <section class="card" style="margin-top:16px">
      <h3 style="margin-top:0">Cómo funciona</h3>
      <ol class="muted">
        <li>Entra con tu teléfono (WhatsApp) o tu email.</li>
        <li>Recibes un código de <strong id="len"></strong> dígitos que caduca en 15 minutos.</li>
        <li>Verificas y listo: reservas en 3 toques.</li>
      </ol>
    </section>
  </main>

  <!-- Modal mínimo para pedir identidad y redirigir a /verify -->
  <div id="loginModal" class="modal-wrap" aria-hidden="true">
    <div class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
      <header>
        <strong id="modalTitle">Acceso de clientes</strong>
        <button id="closeLogin" class="btn">✕</button>
      </header>
      <main>
        <div class="tabs" role="tablist">
          <button class="tab active" id="tabWa" data-channel="whatsapp" aria-selected="true">WhatsApp</button>
          <button class="tab" id="tabEmail" data-channel="email" aria-selected="false">Email</button>
        </div>

        <form id="sendForm">
          <label id="phoneLabel">Teléfono
            <input name="phone" placeholder="+34..." inputmode="tel"/>
          </label>
          <label id="emailLabel" style="display:none">Email
            <input name="email" type="email" placeholder="ejemplo@correo.com"/>
          </label>
          <input type="hidden" name="channel" value="whatsapp"/>
          <button class="btn primary" id="sendBtn">Enviar código</button>
          <div id="sendStatus" class="muted" style="margin-top:6px"></div>
        </form>
      </main>
    </div>
  </div>

  <div id="toast" class="toast"></div>

  <script>
    const $ = (s)=>document.querySelector(s);
    const toast = (m)=>{ const t=$('#toast'); t.textContent=m; t.style.display='block'; setTimeout(()=>t.style.display='none', 2200); };
    document.querySelector('#len').textContent = '<%= (process.env.OTP_LENGTH || 4) %>';

    const modal = $('#loginModal'), openBtn=$('#openLogin'), closeBtn=$('#closeLogin');
    const tabWa=$('#tabWa'), tabEmail=$('#tabEmail');
    const phoneLabel=$('#phoneLabel'), emailLabel=$('#emailLabel');
    const form=$('#sendForm'), sendBtn=$('#sendBtn'), sendStatus=$('#sendStatus');

    function open(){ modal.style.display='flex'; modal.setAttribute('aria-hidden','false'); }
    function close(){ modal.style.display='none'; modal.setAttribute('aria-hidden','true'); }
    function setChannel(ch){
      form.channel.value=ch;
      tabWa.classList.toggle('active', ch==='whatsapp'); tabWa.setAttribute('aria-selected', ch==='whatsapp');
      tabEmail.classList.toggle('active', ch==='email');  tabEmail.setAttribute('aria-selected', ch==='email');
      phoneLabel.style.display = ch==='whatsapp' ? '' : 'none';
      emailLabel.style.display = ch==='email' ? '' : 'none';
    }
    openBtn.onclick=open; closeBtn.onclick=close; modal.addEventListener('click',(e)=>{ if(e.target===modal) close(); });
    tabWa.onclick=()=>setChannel('whatsapp'); tabEmail.onclick=()=>setChannel('email'); setChannel('whatsapp');

    form.addEventListener('submit', async (e)=>{
      e.preventDefault();
      sendBtn.disabled=true; sendStatus.textContent='Enviando...';
      const fd=new FormData(form);
      const body={ channel: fd.get('channel'), name: 'Cliente' };
      if(body.channel==='email'){ body.email=fd.get('email'); if(!body.email){ toast('Pon tu email'); sendBtn.disabled=false; sendStatus.textContent=''; return; } }
      else { body.phone=fd.get('phone'); if(!body.phone){ toast('Pon tu teléfono'); sendBtn.disabled=false; sendStatus.textContent=''; return; } }

      const r=await fetch('/auth/send-code',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});
      const j=await r.json();
      sendBtn.disabled=false; sendStatus.textContent='';
      if(!j.ok){ toast('❌ '+(j.error||'Error enviando')); return; }
      // redirige a la pantalla Square-like
      const identity = body.email || body.phone;
      location.href = j.verify_url || (`/verify?channel=${body.channel}&identity=${encodeURIComponent(identity)}`);
    });
  </script>
</body>
</html>
